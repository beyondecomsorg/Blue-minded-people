{% comment %}
  Shop Fabric By Category — Splide Carousel Section
{% endcomment %}

{{ 'https://cdn.jsdelivr.net/npm/@splidejs/splide@4.1.4/dist/css/splide.min.css' | stylesheet_tag }}

<style>
  #shopify-section-{{ section.id }} {
    background-color: {{ section.settings.bg_color }};
  }

  .sfc-wrap {
    max-width: var(--global-max-container-width-desktop);
    margin: 0 auto;
    padding-left: 20px;
    padding-right: 20px;
    padding-top: var(--section-padding-top);
    padding-bottom: var(--section-padding-bottom);
  }

  @media (max-width: 749px) {
    .sfc-wrap {
      padding-top: var(--section-padding-top-mobile) !important;
      padding-bottom: var(--section-padding-bottom-mobile) !important;
    }
  }

  /* Heading */
  .sfc-heading-block {
    text-align: center;
    margin-bottom: 36px;
  }

  .sfc-heading {
    font-family: var(--font-heading--family);
    font-size: var(--font-h2--size);
    line-height: var(--font-h2--line-height);
    letter-spacing: var(--font-h2--letter-spacing);
    font-weight: var(--font-h2--weight);
    color: var(--color-deep-navy);

    text-transform: uppercase;
  }

  .sfc-subheading {
    font-family: var(--font-body--family);
    font-size: var(--font-paragraph--size);
    color: var(--color-medium-grey);
    margin: 0;
    font-weight: 400;
  }

  /* Splide overrides */
  .sfc-splide .splide__track {
    overflow: visible;
    clip-path: inset(0 0 0 0);
  }

  .sfc-splide .splide__list {
    align-items: stretch;
  }

  /* Card */
  .sfc-card {
    position: relative;
    display: block;
    overflow: hidden;
    text-decoration: none;
    border: 1.5px solid var(--color-border-grey);
    background: var(--color-deep-navy);
  }

  .sfc-card-img {
    width: 100%;
    aspect-ratio: {{ section.settings.image_ratio }};
    display: block;
    overflow: hidden;
  }

  .sfc-card-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: top center;
    display: block;
    transition: transform 0.5s ease;
  }

  .sfc-card:hover .sfc-card-img img {
    transform: scale(1.05);
  }

  /* Overlay at bottom */
  .sfc-card-overlay {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    background: linear-gradient(to top, rgba(11,31,51,0.9) 0%, rgba(11,31,51,0.4) 60%, transparent 100%);
    padding: 40px 16px 18px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .sfc-card-title {
    font-family: var(--font-heading--family);
    font-size: 18px;
    font-weight: 700;
    color: var(--color-off-white);
    margin: 0 0 4px;
    line-height: 1.3;
    text-transform: uppercase;
  }

  .sfc-card-subtitle {
    font-family: var(--font-body--family);
    font-size: 13px;
    font-weight: 400;
    color: var(--color-vintage-stone);
    margin: 0;
  }

  /* Arrows */
  .sfc-arrows {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-top: 24px;
    padding: 0 4px;
  }

  .sfc-arrow-btn {
    width: 48px;
    height: 48px;
    border: 1.5px solid var(--color-border-grey);
    background: var(--color-off-white);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    flex-shrink: 0;
    transition: background 0.2s, border-color 0.2s;
  }

  .sfc-arrow-btn:hover {
    background: var(--color-copper-rivet);
    border-color: var(--color-copper-rivet);
  }

  .sfc-arrow-btn:hover svg {
    stroke: var(--color-off-white);
  }

  .sfc-arrow-btn svg {
    width: 18px;
    height: 18px;
    stroke: var(--color-deep-navy);
    fill: none;
    stroke-width: 2;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  /* Pagination dots */
  .sfc-pagination {
    display: flex;
    align-items: center;
    gap: 8px;
    flex: 1;
    justify-content: center;
    padding: 0 20px;
  }

  .sfc-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--color-border-grey);
    border: none;
    cursor: pointer;
    padding: 0;
    transition: background 0.2s, width 0.3s;
  }

  .sfc-dot.is-active {
    background: var(--color-copper-rivet);
    width: 32px;
    border-radius: 5px;
  }

  /* Splide track clip fix */
  .sfc-splide .splide__track {
    overflow: hidden;
  }
</style>

<div class="sfc-wrap section-wrapper" style="--section-padding-top: {{ section.settings.padding_top }}px; --section-padding-bottom: {{ section.settings.padding_bottom }}px; --section-padding-top-mobile: {{ section.settings.padding_top_mobile }}px; --section-padding-bottom-mobile: {{ section.settings.padding_bottom_mobile }}px;">
  {% if section.settings.heading != blank %}
    <div class="sfc-heading-block">
      <h2 class="sfc-heading">{{ section.settings.heading }}</h2>
      {% if section.settings.subheading != blank %}
        <p class="sfc-subheading">{{ section.settings.subheading }}</p>
      {% endif %}
    </div>
  {% endif %}

  <div
    id="sfc-splide-{{ section.id }}"
    class="splide sfc-splide"
    aria-label="{{ section.settings.heading }}"
  >
    <div class="splide__track">
      <ul class="splide__list">
        {% for block in section.blocks %}
          {% if block.type == 'slide_card' %}
            <li class="splide__slide" {{ block.shopify_attributes }}>
              <a href="{{ block.settings.card_url }}" class="sfc-card">
                <div class="sfc-card-img">
                  {% if block.settings.card_image != blank %}
                    <img
                      src="{{ block.settings.card_image | img_url: '800x' }}"
                      alt="{{ block.settings.card_title }}"
                      loading="lazy"
                      width="800"
                      height="1000"
                    >
                  {% else %}
                    {{ 'collection-1' | placeholder_svg_tag: 'placeholder-svg' }}
                  {% endif %}
                </div>
                <div class="sfc-card-overlay">
                  {% if block.settings.card_title != blank %}
                    <p class="sfc-card-title">{{ block.settings.card_title }}</p>
                  {% endif %}
                  {% if block.settings.card_subtitle != blank %}
                    <p class="sfc-card-subtitle">{{ block.settings.card_subtitle }}</p>
                  {% endif %}
                </div>
              </a>
            </li>
          {% endif %}
        {% endfor %}
      </ul>
    </div>
  </div>

  <!-- Custom Controls -->
  <div class="sfc-arrows">
    <button class="sfc-arrow-btn" id="sfc-prev-{{ section.id }}" aria-label="Previous">
      <svg viewBox="0 0 24 24"><polyline points="15 18 9 12 15 6"></polyline></svg>
    </button>
    <div class="sfc-pagination" id="sfc-dots-{{ section.id }}"></div>
    <button class="sfc-arrow-btn" id="sfc-next-{{ section.id }}" aria-label="Next">
      <svg viewBox="0 0 24 24"><polyline points="9 18 15 12 9 6"></polyline></svg>
    </button>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/@splidejs/splide@4.1.4/dist/js/splide.min.js" defer></script>

<script>
document.addEventListener('DOMContentLoaded', function () {
  var el = document.getElementById('sfc-splide-{{ section.id }}');
  if (!el) return;

  var splide = new Splide(el, {
    type       : 'loop',
    perPage    : {{ section.settings.slides_desktop }},
    perMove    : 1,
    gap        : '{{ section.settings.slide_gap }}px',
    pagination : false,
    arrows     : false,
    breakpoints: {
      1024: { perPage: {{ section.settings.slides_tablet }} },
      640 : { perPage: {{ section.settings.slides_mobile }}, gap: '10px' }
    }
  });

  // Dots
  var dotsWrap = document.getElementById('sfc-dots-{{ section.id }}');
  var totalPages = Math.ceil({{ section.blocks.size }} / {{ section.settings.slides_desktop }});

  for (var i = 0; i < totalPages; i++) {
    var dot = document.createElement('button');
    dot.className = 'sfc-dot' + (i === 0 ? ' is-active' : '');
    dot.setAttribute('aria-label', 'Go to page ' + (i + 1));
    (function(index) {
      dot.addEventListener('click', function () {
        splide.go(index * {{ section.settings.slides_desktop }});
      });
    })(i);
    dotsWrap.appendChild(dot);
  }

  splide.on('moved', function (newIndex) {
    var dots = dotsWrap.querySelectorAll('.sfc-dot');
    var activePage = Math.floor(newIndex / {{ section.settings.slides_desktop }}) % totalPages;
    dots.forEach(function (d, i) {
      d.classList.toggle('is-active', i === activePage);
    });
  });

  // Arrows
  document.getElementById('sfc-prev-{{ section.id }}').addEventListener('click', function () {
    splide.go('<');
  });
  document.getElementById('sfc-next-{{ section.id }}').addEventListener('click', function () {
    splide.go('>');
  });

  splide.mount();
});
</script>

{% schema %}
{
  "name": "Fabric Category Carousel",
  "tag": "section",
  "class": "fabric-category-carousel",
  "settings": [
    {
      "type": "header",
      "content": "Heading"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "SHOP SHIRT FABRIC BY CATEGORY"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Explore premium shirt fabrics tailored for every style and occasion."
    },
    {
      "type": "range",
      "id": "heading_size",
      "label": "Heading Size",
      "min": 18,
      "max": 60,
      "step": 1,
      "unit": "px",
      "default": 36
    },
    {
      "type": "range",
      "id": "subheading_size",
      "label": "Subheading Size",
      "min": 12,
      "max": 24,
      "step": 1,
      "unit": "px",
      "default": 15
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading Color",
      "default": "#111111"
    },
    {
      "type": "color",
      "id": "subheading_color",
      "label": "Subheading Color",
      "default": "#555555"
    },
    {
      "type": "header",
      "content": "Carousel Layout"
    },
    {
      "type": "range",
      "id": "slides_desktop",
      "label": "Slides Visible (Desktop)",
      "min": 2,
      "max": 5,
      "step": 1,
      "default": 4
    },
    {
      "type": "range",
      "id": "slides_tablet",
      "label": "Slides Visible (Tablet)",
      "min": 1,
      "max": 4,
      "step": 1,
      "default": 2
    },
    {
      "type": "range",
      "id": "slides_mobile",
      "label": "Slides Visible (Mobile)",
      "min": 1,
      "max": 3,
      "step": 1,
      "default": 1
    },
    {
      "type": "range",
      "id": "slide_gap",
      "label": "Gap Between Slides",
      "min": 0,
      "max": 40,
      "step": 2,
      "unit": "px",
      "default": 16
    },
    {
      "type": "select",
      "id": "image_ratio",
      "label": "Image Aspect Ratio",
      "default": "3/4",
      "options": [
        { "value": "1/1", "label": "Square (1:1)" },
        { "value": "3/4", "label": "Portrait (3:4)" },
        { "value": "2/3", "label": "Tall (2:3)" },
        { "value": "4/5", "label": "Standard (4:5)" }
      ]
    },
    {
      "type": "header",
      "content": "Card Text"
    },
    {
      "type": "range",
      "id": "card_title_size",
      "label": "Card Title Size",
      "min": 12,
      "max": 26,
      "step": 1,
      "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "card_subtitle_size",
      "label": "Card Subtitle Size",
      "min": 10,
      "max": 20,
      "step": 1,
      "unit": "px",
      "default": 13
    },
    {
      "type": "color",
      "id": "card_border_color",
      "label": "Card Border Color",
      "default": "#dddddd"
    },
    {
      "type": "header",
      "content": "Arrow Buttons"
    },
    {
      "type": "color",
      "id": "arrow_bg_color",
      "label": "Arrow Background",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "arrow_border_color",
      "label": "Arrow Border",
      "default": "#cccccc"
    },
    {
      "type": "color",
      "id": "arrow_color",
      "label": "Arrow Icon Color",
      "default": "#111111"
    },
    {
      "type": "color",
      "id": "arrow_hover_bg",
      "label": "Arrow Hover Background",
      "default": "#111111"
    },
    {
      "type": "color",
      "id": "arrow_hover_color",
      "label": "Arrow Hover Icon Color",
      "default": "#ffffff"
    },
    {
      "type": "header",
      "content": "Pagination Dots"
    },
    {
      "type": "color",
      "id": "dot_active_color",
      "label": "Active Dot Color",
      "default": "#111111"
    },
    {
      "type": "color",
      "id": "dot_inactive_color",
      "label": "Inactive Dot Color",
      "default": "#cccccc"
    },
    {
      "type": "header",
      "content": "Typography"
    },
    {
      "type": "select",
      "id": "font_family",
      "label": "Font Family",
      "default": "inherit",
      "options": [
        { "value": "inherit", "label": "Theme Default" },
        { "value": "Helvetica, Arial, sans-serif", "label": "Helvetica" },
        { "value": "'Times New Roman', serif", "label": "Times New Roman" },
        { "value": "Georgia, serif", "label": "Georgia" }
      ]
    },
    {
      "type": "header",
      "content": "Background & Padding"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Section Background",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "container_width",
      "label": "Max Container Width",
      "min": 800,
      "max": 1600,
      "step": 20,
      "unit": "px",
      "default": 1400
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Section top padding — desktop (px)",
      "min": 0,
      "max": 120,
      "step": 4,
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Section bottom padding — desktop (px)",
      "min": 0,
      "max": 120,
      "step": 4,
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "label": "Section top padding — mobile (px)",
      "min": 0,
      "max": 100,
      "step": 2,
      "default": 30
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "label": "Section bottom padding — mobile (px)",
      "min": 0,
      "max": 100,
      "step": 2,
      "default": 30
    }
  ],
  "blocks": [
    {
      "type": "slide_card",
      "name": "Slide Card",
      "settings": [
        {
          "type": "image_picker",
          "id": "card_image",
          "label": "Card Image"
        },
        {
          "type": "text",
          "id": "card_title",
          "label": "Title",
          "default": "Pure Linen Shirt Fabric (62)"
        },
        {
          "type": "text",
          "id": "card_subtitle",
          "label": "Subtitle / Tag",
          "default": "First-class quality"
        },
        {
          "type": "url",
          "id": "card_url",
          "label": "Card Link URL"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Fabric Category Carousel",
      "blocks": [
        { "type": "slide_card", "settings": { "card_title": "Pure Linen Shirt Fabric (62)", "card_subtitle": "First-class quality" } },
        { "type": "slide_card", "settings": { "card_title": "Pure Cotton Shirt Fabric (543)", "card_subtitle": "top-notch fabric" } },
        { "type": "slide_card", "settings": { "card_title": "Linen Cotton Mix Blended Shirt Fabric (110)", "card_subtitle": "best in-house" } },
        { "type": "slide_card", "settings": { "card_title": "Wrinkle Free Shirt Fabric (104)", "card_subtitle": "NEW - Save up to 30%!" } },
        { "type": "slide_card", "settings": { "card_title": "Oxford Shirt Fabric (88)", "card_subtitle": "Classic weave" } },
        { "type": "slide_card", "settings": { "card_title": "Dobby Shirt Fabric (74)", "card_subtitle": "Textured premium" } }
      ]
    }
  ]
}
{% endschema %}
